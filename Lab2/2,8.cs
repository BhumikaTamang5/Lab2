//8) Write a program in C$ to binary operator.

using System;

class BinaryOperatorsDemo
{
    /*
    static void Main()
    {
        int num1 = 12; // 12 in binary: 1100
        int num2 = 5;  // 5 in binary: 0101

        // Binary AND
        int andResult = num1 & num2;
        Console.WriteLine($"{num1} & {num2} = {andResult} (Binary: {Convert.ToString(andResult, 2)})");

        // Binary OR
        int orResult = num1 | num2;
        Console.WriteLine($"{num1} | {num2} = {orResult} (Binary: {Convert.ToString(orResult, 2)})");

        // Binary XOR
        int xorResult = num1 ^ num2;
        Console.WriteLine($"{num1} ^ {num2} = {xorResult} (Binary: {Convert.ToString(xorResult, 2)})");

        // Left Shift
        int leftShiftResult = num1 << 1;
        Console.WriteLine($"{num1} << 1 = {leftShiftResult} (Binary: {Convert.ToString(leftShiftResult, 2)})");

        // Right Shift
        int rightShiftResult = num1 >> 1;
        Console.WriteLine($"{num1} >> 1 = {rightShiftResult} (Binary: {Convert.ToString(rightShiftResult, 2)})");
        Console.ReadKey();
    } */
}
