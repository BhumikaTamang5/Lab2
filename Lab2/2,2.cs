using System;
/*

// Define Namespace1
namespace Namespace1
{
    // Student class definition
    public class Student
    {
        // Fields of the Student class
        public string Name;
        public int Age;
        public string Address;

        // Constructor to initialize the Student object
        public Student(string name, int age, string address)
        {
            Name = name;
            Age = age;
            Address = address;
        }

        // Method to display student details
        public void DisplayStudentDetails()
        {
            Console.WriteLine("Student Details:");
            Console.WriteLine($"Name: {Name}");
            Console.WriteLine($"Age: {Age}");
            Console.WriteLine($"Address: {Address}");
        }
    }
}

// Define Namespace2
namespace Namespace2
{
    // Main Program class
    class Program
    {
        
        static void Main(string[] args)
        {
            // Use the Student class from Namespace1
            Namespace1.Student emp1 = new Namespace1.Student("Bhumika Tamang", 21, "Lalitpur");

            // Accessing the method of Student class
            emp1.DisplayStudentDetails();

            // Waiting for user input before closing the console window
            Console.ReadLine();
        } 
    }
        } 
} */
